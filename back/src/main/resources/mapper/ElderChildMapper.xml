<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuqilong.back.mapper.ElderChildMapper">

    <resultMap id="BaseResultMap" type="com.zhuqilong.back.entity.ElderChild">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="elder_id" property="elderId" jdbcType="BIGINT"/>
        <result column="child_id" property="childId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <resultMap id="ElderResultMap" type="com.zhuqilong.back.entity.Elder">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
        <result column="gender" property="gender" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="grid_group_id" property="gridGroupId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <insert id="insert" parameterType="com.zhuqilong.back.entity.ElderChild" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO elder_child (elder_id, child_id, created_at)
        VALUES (#{elderId}, #{childId}, #{createdAt})
    </insert>

    <update id="updateById" parameterType="com.zhuqilong.back.entity.ElderChild">
        UPDATE elder_child
        SET elder_id = #{elderId},
            child_id = #{childId},
            created_at = #{createdAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM elder_child WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT id, elder_id, child_id, created_at
        FROM elder_child
        WHERE id = #{id}
    </select>

    <select id="selectEldersByChildId" parameterType="java.lang.Long" resultMap="ElderResultMap">
        SELECT e.id, e.name, e.age, e.gender, e.status, e.grid_group_id, e.created_at
        FROM elder e
        INNER JOIN elder_child ec ON e.id = ec.elder_id
        WHERE ec.child_id = #{childId}
        ORDER BY e.created_at DESC
    </select>

    <select id="selectByChildId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT id, elder_id, child_id, created_at
        FROM elder_child
        WHERE child_id = #{childId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByElderId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT id, elder_id, child_id, created_at
        FROM elder_child
        WHERE elder_id = #{elderId}
        ORDER BY created_at DESC
    </select>

    <delete id="deleteByChildAndElder" parameterType="map">
        DELETE FROM elder_child 
        WHERE child_id = #{childId} AND elder_id = #{elderId}
    </delete>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, elder_id, child_id, created_at
        FROM elder_child
    </select>

</mapper>
